// import Typed from 'typed.js';

const options = {
    strings: ["Hey, I’m Michael Zhu and I love building beautiful websites."],
    typeSpeed: 50,
    smartBackspace: true,
    loopCount: Infinity,
    fadeOut: true,
    cursorChar: "",
    // loop: true
};

const typed = new Typed ('#intro-typing-effect', options);